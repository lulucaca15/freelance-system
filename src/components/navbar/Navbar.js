const Navbar = () => {
    return (
        <nav className='navbar'>
            <div className='nav_icon' onClick={() => openSidebar}>
                
            </div>

        </nav>
    )
}

export default Navbar